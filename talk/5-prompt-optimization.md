# Lesson 4: Prompt optimization from logs

The agent kept hitting the same avoidable issues every run, burning tokens.

Fix: review session logs with Claude Code, identify repeated patterns, update the prompt to preempt them.

This could be a scheduled "meta-agent" job.
